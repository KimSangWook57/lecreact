import React from 'react'; // 수정금지
import ReactDOM from 'react-dom/client'; // 수정금지
import './index.css';
import App from './App'; // 수정금지

// 가상 돔 생성
const root = ReactDOM.createRoot(document.getElementById('root'));
// 렌더링


root.render(
    <App />
);
